name: Deploy Images to GHCR

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@main

      - name: 'Set Timestamp'
        id: vars
        run: echo "TIMESTAMP=$(date +'%Y%m%d-%H%M%S')" >> $GITHUB_ENV

      - name: 'Login to GitHub Container Registry'
        uses: docker/login-action@v3 # Updated to v3 for better stability
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 'Build and Push Inventory Image'
        run: |
          # Define the base image name
          IMAGE_NAME=ghcr.io/${{ github.repository_owner }}/ollama-proxy
          
          # Build with both tags
          docker build . \
            --tag ${IMAGE_NAME}:latest \
            --tag ${IMAGE_NAME}:${{ env.TIMESTAMP }}
          
          # Push both tags
          docker push ${IMAGE_NAME}:latest
          docker push ${IMAGE_NAME}:${{ env.TIMESTAMP }}